EASISHOP_API_USERNAME=your_username
EASISHOP_API_PASSWORD=your_password

# Demo offline catalog (barcodeIndex JSON). true = skip live search entirely.
# If unset, live API is tried first; empty/error responses fall back to local.
EASISHOP_USE_LOCAL_CATALOG=true
